# .env.example
# Copy to .env.local for development, or set in hosting platform for production
# All PUBLIC_KILN_* values appear in /config.json

# === REQUIRED: LLM CONFIGURATION ===
PUBLIC_KILN_LLM_URL=https://openrouter.ai/api/v1
PUBLIC_KILN_MODEL=x-ai/grok-4-fast:free
PUBLIC_KILN_TEMPERATURE=0.8
KILN_API_KEY=sk-...

# === REQUIRED: FHIR CONFIGURATION ===
PUBLIC_KILN_FHIR_BASE_URL=https://example.org/fhir
# Leave blank to auto-detect same-origin deployed server for validation services
PUBLIC_KILN_VALIDATION_SERVICES_URL=
PUBLIC_KILN_FHIR_GEN_CONCURRENCY=1

# === OPTIONAL: APP CONFIGURATION ===
PUBLIC_KILN_DEBUG_MODE=true
PUBLIC_KILN_MAX_RETRIES=3
PUBLIC_KILN_LLM_MAX_CONCURRENCY=4
PUBLIC_KILN_BASE_PATH=/

# === SERVER/HEADLESS CONFIG ===
# These variables are consumed by the Bun server and the headless CLI (not exposed to the browser).
# Provide values as needed when running outside the UI. Secrets should stay out of version control.
KILN_DATA_DIR=kiln-data
#KILN_BASE_URL=https://openrouter.ai/api/v1
#KILN_MODEL=openai/gpt-4o-mini
#KILN_TEMPERATURE=0.2
#KILN_FHIR_BASE_URL=https://example.org/fhir
#KILN_VALIDATION_URL=
#KILN_FHIR_CONCURRENCY=1
#KILN_MAX_RETRIES=3
#KILN_LLM_MAX_CONCURRENCY=4
#KILN_DEBUG=false
#KILN_EXPORT_DIR=kiln-data/exports
#KILN_STORAGE_MODE=filesystem
